CREATE TABLES

  CREATE TABLE IF NOT EXISTS Products (
  product_id SERIAL PRIMARY KEY,
  product_name VARCHAR NOT NULL UNIQUE,
  company_id INT,
  description VARCHAR,
  price FLOAT,
  active BOOLEAN DEFAULT true
  );

  CREATE TABLE IF NOT EXISTS Warranties (
  warranty_id SERIAL PRIMARY KEY,
  warranty_months INT NOT NULL,
  product_id INT,
  );
  
  CREATE TABLE IF NOT EXISTS Companies (
  company_id SERIAL PRIMARY KEY,
  company_name VARCHAR NOT NULL UNIQUE,
  active BOOLEAN DEFAULT true
  );
  
  CREATE TABLE IF NOT EXISTS Categories (
  category_id SERIAL PRIMARY KEY,
  category_name VARCHAR NOT NULL UNIQUE,
  active BOOLEAN DEFAULT true
  );
  
  CREATE TABLE IF NOT EXISTS ProductsCategoriesXref (
  product_id INT,
  category_id INT
  );

CREATE
  - INSERT INTO Companies (company_name) VALUES ('corp');
  - INSERT INTO Categories (category_name) VALUES ('cat1');
  - INSERT INTO Products (product_name, company_id, description, price) VALUES ('prod1', 1, 'description', 9.99);
  - INSERT INTO Warranties (warranty_months, product_id) VALUES (1, 1);
  - INSERT INTO ProductsCategoriesXref (product_id, category_id) VALUES (1, 1);

READ
  - SELECT * FROM Companies;
  - SELECT * FROM Categories;
  - SELECT * FROM Products;
  - SELECT * FROM Warranties;
  - SELECT * FROM Products WHERE active = True;
  - SELECT * FROM Products WHERE company_id = 1;
  - SELECT * FROM Companies WHERE company_id = 1;
  - SELECT * FROM Categories JOIN ProductsCategoriesXref ON category_id = category_id JOIN Products ON product_id = product_id WHERE category_id = 1;
  - SELECT * FROM Products JOIN Warranties ON product_id = product_id JOIN ProductsCategoriesXref ON product_id = product _id JOIN Categories ON category_id = category_id WHERE product_id = 1;
  - SELECT * FROM Warranties WHERE warranty_id = 1;

UPDATE
  - UPDATE Companies SET company_name = 'company' WHERE company_id = 1;
  - UPDATE Categories SET category_name = 'names' WHERE category_id = 1;
  - UPDATE Products SET company_id = 2 WHERE product_id = 1;
  - UPDATE Warranties SET warranty_months = 3 WHERE warranty_id = 1;
  - UPDATE ProductsCategoriesXref SET category_id = 2 WHERE product_id = 1 AND category_id = 1;

DELETE
  - DELETE FROM Products WHERE product_id = 1; DELETE FROM Warranties WHERE product_id = 1; DELETE FROM ProductsCategoriesXref WHERE product_id = 1;
  - DELETE FROM Categories WHERE category_id = 1; DELETE FROM ProductsCategoriesXref WHERE category_id = 1; 
  - DELETE FROM Companies WHERE company_id = 1; DELETE FROM Products WHERE company_id = 1; 
  - DELETE FROM Warranties WHERE warranty_id = 1;
